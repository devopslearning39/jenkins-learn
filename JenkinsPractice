pipeline {
   agent {
        node {
            label 'AGENT-1'
        }
   }

   environment { 
        NAME = 'praveen'
    }


    stages {
        stage('Build') {
            steps {
                echo "This is Build"
            }
        }

        stage('Deploy') {
            steps {
                echo "This is Deploy"
            }
        }
        stage('Prod') {
            steps {
                echo "Webhookconfigured properly"
            }
        }
        stage('Fetchng env var') {
            steps {
                echo "Person is $NAME"
            }
        }
    }
    post {
        failure {
            echo "Jella, script failed pls check.."
        }
        always {
            echo "I will always say Hello again!..i don't need success or failure.."
        }
        success {
            echo "I will always say Hello if the build success only.."
        }
    }
}