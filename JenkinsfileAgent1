pipeline {
    agent {
        node {
            label 'AGENT-1'
        }
    }

    options {
        ansiColor('xterm')
    }
    parameters {
        choice(name: 'Env', choices: ['Dev', 'Prod'], description: 'Please select which env you wanted to deploy ?')
    }
    stages {
        stage('Testing when condtion') {
            when {
                expression { params.Env == 'Dev' }
            }
            steps {
                echo 'You have selected Dev env, So we are deploying in dev env.'
            }
            when {
                expression { params.Env == 'Prod' }
            }
            steps {
                echo 'You have selected Prod env, So we cannot deploy in the prod env'
            }
        }
    }
}