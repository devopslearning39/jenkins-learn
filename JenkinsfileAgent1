pipeline {
    agent {
        node {
            label 'AGENT-1'
        }
    }

    options {
        ansiColor('xterm')
    }
    parameters {
        choice(name: 'Env', choices: ['Dev', 'Prod'], description: 'Please select which env you wanted to deploy ?')
    }
    stages {
        stage('Dev env build deploy') {
            when {
                expression { params.Env == 'Dev' }
            }
            input {
                message "Should we continue to deploy in Dev env ?"
                ok "Yes, we should."
            }
            steps {
                echo 'You have selected Dev env, So we are deploying in dev env.'
            }
        }

        stage('Prod env build deploy') {
            when {
                expression { params.Env == 'Prod' }
            }
            input {
                message "Should we continue to deploy in Prod env ?"
                ok "Yes, we should."
            }  
            steps {
                echo 'You have selected Prod env..'
            }
        }
    }
}